{
  "name": "forum-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute forum-db --local --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.1.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
